{
  "name": "asiora",
  "version": "0.1.0",
  "description": "Asiora SEA FSI transformation portfolio \u2014 shared scaffolds + identity blueprint",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "lint": "eslint .",
    "lint:openapi": "pnpm --filter @asiora/openapi-core lint:domains",
    "bundle:openapi": "pnpm --filter @asiora/openapi-core bundle:domains",
    "build": "pnpm --filter @asiora/core build && pnpm --filter @asiora/services build && pnpm --filter @asiora/adapters build && pnpm --filter @asiora/api-server build",
    "build:web": "pnpm --filter @asiora/webapp build",
    "dev:api": "PORT=4010 pnpm --filter @asiora/api-server dev",
    "dev:web": "pnpm --filter @asiora/webapp dev",
    "codegen:core": "node scripts/sync-codegen-paths.mjs && PYTHONPATH=.codegen/codegen/src python3 -m zero_codegen.cli.main generate --all --layers core --config .codegen/.zero-codegen-merged.json --skip-build",
    "codegen:identity": "node scripts/sync-codegen-paths.mjs && PYTHONPATH=.codegen/codegen/src python3 -m zero_codegen.cli.main generate --domain identity --config .codegen/.zero-codegen-merged.json --skip-build",
    "codegen:paths": "node scripts/sync-codegen-paths.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@types/node": "^20.0.0",
    "eslint": "^9.0.0",
    "prettier": "^3.8.1",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.55.0"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9.0.0"
  }
}
