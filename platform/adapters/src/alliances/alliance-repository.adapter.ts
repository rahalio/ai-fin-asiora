/**
 * AllianceRepositoryAdapter - Port Adapter Implementation
 *
 * Generated by Port Adapter Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: alliances
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * AllianceRepositoryAdapter
 *
 * DDD: Infrastructure adapter that implements AllianceRepository port.
 * Handles infrastructure concerns (HTTP, database, external APIs).
 * Encapsulates Ddb creation - api-server passes dynamoClient only.
 */

import type {
  AllianceRepository,
} from "@asiora/services/alliances";
import type { AdapterDynamoDBClient } from "../_shared/dynamodb-client-types.js";
import { AllianceRepositoryDdb } from "./alliance-repository.ddb.js";

/**
 * AllianceRepositoryAdapter
 *
 * Implements AllianceRepository port for infrastructure layer.
 */
export class AllianceRepositoryAdapter implements AllianceRepository {
  private readonly ddb: AllianceRepositoryDdb;

  constructor(
    private readonly dynamoClient: AdapterDynamoDBClient
  ) {
    this.ddb = new AllianceRepositoryDdb(this.dynamoClient);
  }

  async listAllianceScores(input: Parameters<AllianceRepository['listAllianceScores']>[0]): Promise<Awaited<ReturnType<AllianceRepository['listAllianceScores']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.listAllianceScores(input);
  }

  async createAllianceScore(input: Parameters<AllianceRepository['createAllianceScore']>[0]): Promise<Awaited<ReturnType<AllianceRepository['createAllianceScore']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.createAllianceScore(input);
  }

  async getAllianceScore(input: Parameters<AllianceRepository['getAllianceScore']>[0]): Promise<Awaited<ReturnType<AllianceRepository['getAllianceScore']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.getAllianceScore(input);
  }
}
