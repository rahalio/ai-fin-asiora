/**
 * ExecuteWaiveEthicsFlag - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: gates
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteWaiveEthicsFlag Use Case
 *
 * DDD: Application use case for get Waiver.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { WaiveEthicsFlagInput, WaiveEthicsFlagOutput } from "../dto/waiver.dto";
import type { ExecutionContextService, IdGeneratorService } from "@asiora/services/_shared/index.js";
import type { WaiverRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteWaiveEthicsFlag {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly waiver: WaiverRepository
  ) {}

  async execute(input: WaiveEthicsFlagInput): Promise<WaiveEthicsFlagOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.gatId();

    // All field names extracted from OpenAPI spec: waiverRationale
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).ethicsFlagId) {
      throw new ValidationError("ethicsFlagId is required");
    }
    const ethicsflagId = (input as any).ethicsFlagId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.waiver.waiveEthicsFlag({ ...input, ethicsFlagId: ethicsflagId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${ethicsflagId}`);
    }

    return entity as WaiveEthicsFlagOutput;
  }
}
