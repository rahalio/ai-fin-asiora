/**
 * ExecuteGetAllianceScore - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: alliances
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetAllianceScore Use Case
 *
 * DDD: Application use case for get Alliance.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetAllianceScoreInput, GetAllianceScoreOutput } from "../dto/alliance.dto";
import type { ExecutionContextService, IdGeneratorService } from "@asiora/services/_shared/index.js";
import type { AllianceRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetAllianceScore {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly alliance: AllianceRepository
  ) {}

  async execute(input: GetAllianceScoreInput): Promise<GetAllianceScoreOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.alnId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).allianceId) {
      throw new ValidationError("allianceId is required");
    }
    const allianceId = (input as any).allianceId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.alliance.getAllianceScore({ allianceId: allianceId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${allianceId}`);
    }

    return entity as GetAllianceScoreOutput;
  }
}
