/**
 * SetInsuranceScenario handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: scenarios
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { setInsuranceScenario } from "@asiora/api-server/domains/scenarios/handlers";
import type { ScenariosDDDDependencies } from "@asiora/api-server/domains/scenarios";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@asiora/tests/shared";
import { SCENARIOS_USE_CASES } from "../__factories__/use-cases.js";

describe("SetInsuranceScenario", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ScenariosDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ScenariosDDDDependencies>(
      SCENARIOS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with updated entity", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const input = {} as any;
    const updatedEntity = { ...entity, ...input };
    const useCaseResult = { data: updatedEntity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.params = { initiativeId: entity.id };
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeSetInsuranceScenario, "execute").mockResolvedValue(useCaseResult);

    await setInsuranceScenario(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeSetInsuranceScenario.execute).toHaveBeenCalledWith(expect.objectContaining({ initiativeId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
