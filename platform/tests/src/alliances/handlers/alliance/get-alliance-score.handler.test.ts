/**
 * GetAllianceScore handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: alliances
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getAllianceScore } from "@asiora/api-server/domains/alliances/handlers";
import type { AlliancesDDDDependencies } from "@asiora/api-server/domains/alliances";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@asiora/tests/shared";
import { ALLIANCES_USE_CASES } from "../__factories__/use-cases.js";

describe("GetAllianceScore", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: AlliancesDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<AlliancesDDDDependencies>(
      ALLIANCES_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetAllianceScore, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { allianceId: entity.id };

    await getAllianceScore(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetAllianceScore.execute).toHaveBeenCalledWith(expect.objectContaining({ allianceId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { allianceId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetAllianceScore, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getAllianceScore(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
